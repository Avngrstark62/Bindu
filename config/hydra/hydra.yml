dsn: postgres://hydra:hydra@postgres:5432/hydra?sslmode=disable

urls:
  self:
    issuer: http://localhost:4444

serve:
  public:
    port: 4444
  admin:
    port: 4445

secrets:
  system:
    - super-secret-system-key-change-in-prod

strategies:
  access_token: jwt

ttl:
  access_token: 1h
  refresh_token: 720h

log:
  level: info

oauth2:
  expose_internal_errors: true
